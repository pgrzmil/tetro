export class PositionMatrixItem {
    public deltaX: number = 0;
    public deltaY: number = 0;

    constructor(deltaX: number, deltaY: number) {
        this.deltaX = deltaX;
        this.deltaY = deltaY;
    }
}
